// broswer sends a get request -> when it needs something
// when we summit data it sends a post request
// http have some methods 
get -> lists all the users. 
post -> post about the users

// What is data stored?
-> data storage is when your programme, keeps the information. 
after other request
// 2 ways
// use a real data base. 
// in memory storage
